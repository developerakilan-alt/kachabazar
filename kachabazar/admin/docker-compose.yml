services:
  kachabazar_admin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kachabazar_admin
    restart: unless-stopped
    environment:
      VITE_API_TARGET: http://kachabazar_backend:5056
    networks:
      - default

networks:
  default:
    external:
      name: bf
