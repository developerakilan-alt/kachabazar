services:
  stage_webserver:
    image: nginx:latest
    container_name: stage_webserver
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    networks:
      - bf

  kachabazar_backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kachabazar_backend
    environment:
      PUBLIC_API_URL: http://192.168.29.108
    volumes:
      - ./public:/app/public
    restart: unless-stopped
    networks:
      - bf

networks:
  bf:
    external: true
    name: bf
